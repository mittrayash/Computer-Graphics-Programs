#include <stdio.h>
#include <stdlib.h>
#include <graphics.h>
#include <conio.h>

#define Round(x) ((int)(x+0.5))
int main()
{
    float xIncrement, yIncrement, x, y, step;
    int dx, dy, i, x1, x2, y1, y2;

    int gDriver = DETEC


    return 0;
}
